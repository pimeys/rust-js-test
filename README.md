# rust-js-test

## Install and try

Install [neon](https://neon-bindings.com/) first.

Compile the code:

```bash
> neon build --release
```

Open `node` and execute the JavaScript code from `index.js`:

```javascript
> require('.')
```
